#!/bin/bash
echo "starting"
for i in `cat /etc/eucalyptus/eucalyptus.conf | grep "NODES" | awk -F'"' '{print $2}'`
do
	echo "NODE: $i"
	ssh -i ~/id_dsa.plinga root@$i "virsh list;iscsiadm -m session -P 0 2>&1;"
	echo "END NODE: $i"
done
echo "done"
